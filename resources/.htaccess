# Tailwind source input — the built output public/assets/css/app.build.css
# is what's actually served; this source directory never needs to be.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
